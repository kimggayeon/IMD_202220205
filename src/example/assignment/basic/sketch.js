function setup() {
  setCanvasContainer('p5-canvas', 3, 2, true);
  background('white');
}

function draw() {
  background('#fecec0');

  fill(255);
  rectMode(CORNER);
  stroke(0);
  strokeweight(1);
}
